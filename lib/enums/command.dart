/// Enumeration representing the available commands in the application.
enum Command {
  /// Represents the command for calculating **Intrinsic Value**.
  iv,

  /// Represents the command for calculating **Annual Growth**.
  ag,

  /// Represents an invalid command
  none,
}
